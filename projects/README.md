# Projects

One folder per active workstream. Each folder has a `README.md` capturing status, dates, and key context. The assistant looks here first when you reference a specific client or initiative.

See `example-event-launch/` for the shape. Delete it once you have real projects.
